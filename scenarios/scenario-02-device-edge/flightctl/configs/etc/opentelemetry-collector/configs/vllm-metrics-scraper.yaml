receivers:
  prometheus:
    config:
      scrape_configs:
        - job_name: 'vllm-metrics'
          scrape_interval: 10s
          static_configs:
            - targets: ['0.0.0.0:8000']
service:
  pipelines:
    metrics:
      receivers: [prometheus]