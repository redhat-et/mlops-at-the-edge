apiVersion: v1
kind: Namespace
metadata:
  name: openshift-operators
  labels:
    app.kubernetes.io/part-of: mlops-edge
    app.kubernetes.io/component: openshift-pipelines
    scenario: scenario-01-full-openshift
  # Note: openshift-operators namespace typically exists by default
  # This manifest ensures it exists with proper labels

